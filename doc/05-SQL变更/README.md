# 05-SQL变更

本目录用于放后续需求产生的增量 SQL。建议按年份或版本分目录，并使用日期加序号命名，便于追踪和执行。

建议命名：

```text
2026/2026-05-26-001-add-example-field.sql
```

每个 SQL 文件建议在头部写明：

```sql
-- 日期：
-- 模块：
-- 目的：
-- 是否可重复执行：
-- 影响表：
-- 回滚方式：
```
