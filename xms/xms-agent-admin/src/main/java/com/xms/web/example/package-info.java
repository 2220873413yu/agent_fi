/**
 * XMS 后台任务标准示例包。
 *
 * <p>本包只用于展示 Quartz 任务入口和后台结算派发的推荐组织方式。
 * 示例类不得添加 {@code @Component}、{@code @Service}、{@code @Scheduled} 等 Spring 注解，
 * 不参与生产运行，只作为新业务复制和改造的参考模板。</p>
 */
package com.xms.web.example;
