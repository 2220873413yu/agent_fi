/**
 * XMS 基础业务标准示例包。
 *
 * <p>本包只用于展示钱包、订单、异步队列、充值提现和批量结算的推荐写法。
 * 示例类不得添加 {@code @Service}、{@code @Component}、{@code @RestController} 等 Spring 注解，
 * 不参与生产运行，只作为新业务复制和改造的参考模板。</p>
 */
package com.xms.dao.example;
